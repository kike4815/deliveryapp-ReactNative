import { StyleSheet } from "react-native";

export const deliveryStyles = StyleSheet.create({
  flex: {
    flex: 1,
  },
  priceCard: {
    marginEnd: 15,
    fontWeight: "bold",
    fontSize: 16,
  },
});
